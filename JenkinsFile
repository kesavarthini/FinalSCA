 properties([
  buildDiscarder(logRotator(numToKeepStr: '1')),
  disableConcurrentBuilds(),
])
pipeline {
	options {
        disableConcurrentBuilds()
      }
    agent any
	  stages {
          stage('Build') {
        sh 'mvn -B -V -U -e clean package'
    }
        }
      }
}